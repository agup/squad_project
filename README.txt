The data should be stored in ~/data/squad (so the file train-v1-1.json should be in this directory for example)
glove vectors should be stored in ~/data/glove (so the file glove.6B.100d.txt should be in this directory for example)
two directories will need to be created in the same directory as this file


out/basic/00

data/squad


